# AIML Strength Tracker Frontend

This React application provides the user interface for AIML Department to manage student strength tracking across semesters.

## Available Scripts

1. `npm install` – install dependencies
2. `npm start` – run development server at http://localhost:3000
3. `npm build` – create production build
4. `npm test` – run unit tests